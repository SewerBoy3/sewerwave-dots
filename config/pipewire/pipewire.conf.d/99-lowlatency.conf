# sewerwave-dots — PipeWire low-latency defaults for 4 GB RAM / integrated audio

context.properties = {
    default.clock.rate        = 48000
    default.clock.quantum     = 256
    default.clock.min-quantum = 128
    default.clock.max-quantum = 1024
}
